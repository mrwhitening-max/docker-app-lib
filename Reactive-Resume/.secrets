# ============================================
# REACTIVE RESUME CONFIGURATION
# ============================================

# ============================================
# APP CONFIGURATION
# ============================================

# Oeffentliche URL der App (IP oder Domain des Proxmox-Hosts)
# Wird vom Browser verwendet um die App zu erreichen
# Beispiel: http://192.168.1.50:3000, https://resume.deine-domain.de
# WICHTIG: Kein trailing slash!
APP_URL=

# Interne URL der App fuer den Printer-Service (Docker-intern)
# Wird von Browserless verwendet um den Lebenslauf zu rendern
# Immer: http://reactive_resume:3000 (Docker Service Name)
# NICHT aendern, ausser du benennst den Service um!
PRINTER_APP_URL=http://reactive_resume:3000

# ============================================
# AUTHENTICATION
# ============================================

# Geheimer Schluessel fuer JWT-Token Signierung
# Generieren mit: openssl rand -base64 32
# WICHTIG: Sicheres, zufaelliges Secret verwenden! Nicht ins Git committen!
AUTH_SECRET=

# ============================================
# PRINTER / BROWSERLESS CONFIGURATION
# ============================================

# Token fuer die Browserless Chrome API
# Schuetzt den Browserless-Endpoint vor unbefugtem Zugriff
# Generieren mit: openssl rand -hex 16
# WICHTIG: Wird sowohl im browserless-Service als auch im PRINTER_ENDPOINT benoetigt!
BROWSERLESS_TOKEN=

# ============================================
# DATABASE CONFIGURATION
# ============================================

# PostgreSQL Datenbankname
# Beispiel: postgres, reactive_resume, resume_db
POSTGRES_DB=postgres

# PostgreSQL Benutzername
# Beispiel: postgres, rr_user
POSTGRES_USER=postgres

# PostgreSQL Passwort
# WICHTIG: Sicheres Passwort verwenden! Nicht ins Git committen!
POSTGRES_PASSWORD=

# ============================================
# STORAGE CONFIGURATION (SeaweedFS / S3)
# ============================================

# Access Key fuer SeaweedFS S3-API
# Muss in seaweedfs, seaweedfs_create_bucket und reactive_resume identisch sein!
# Beispiel: seaweedfs, mein-access-key
S3_ACCESS_KEY=seaweedfs

# Secret Key fuer SeaweedFS S3-API
# Muss in seaweedfs, seaweedfs_create_bucket und reactive_resume identisch sein!
# Beispiel: seaweedfs, mein-geheimes-secret
S3_SECRET_KEY=seaweedfs

# S3 Bucket Name
# Wird beim Start automatisch erstellt (via seaweedfs_create_bucket)
# Beispiel: reactive-resume, resumes, storage
S3_BUCKET=reactive-resume

# Oeffentliche URL des SeaweedFS S3-Endpoints (fuer Browser-Downloads)
# Der Browser laedt PDFs direkt von dieser URL herunter
# Beispiel: http://192.168.1.50:9000
# WICHTIG: Muss von aussen (Browser) erreichbar sein! Nicht die interne Docker-URL!
S3_URL=