# ============================================
# CONSUL AGENT CONFIGURATION
# ============================================

# Version des Consul Docker Images
# Beispiel: latest, 1.22.3, 1.21.0
# Empfohlung: latest (für automatische Updates)
CONSUL_VERSION

# IP-Adresse des Consul Servers (wo der zentrale Consul Server läuft)
# Dies ist die IP des LXC mit dem Consul Server
# Beispiel: 192.168.2.20, 10.0.0.5
CONSUL_SERVER_IP

# Eindeutiger Name dieses Consul Nodes im Cluster
# Wird in der Consul UI angezeigt
# Beispiel: lxc-n8n, lxc-01, lxc-databases, lxc-dockhand
# Regel: Muss eindeutig sein! Keine zwei Nodes mit gleichem Namen
CONSUL_NODE_NAME

# Name des Consul Datacenters
# Alle Nodes müssen den gleichen Datacenter-Namen haben!
# Beispiel: homelab, dc1, production, europe
CONSUL_DATACENTER

# Log-Level für Consul Agent
# Optionen: TRACE, DEBUG, INFO, WARN, ERROR
# Beispiel: INFO (Standard), DEBUG (für Troubleshooting)
CONSUL_LOG_LEVEL

# ============================================
# NETWORK CONFIGURATION
# ============================================

# Netzwerk-Interface für automatische IP-Erkennung
# Das Script ermittelt die IP von diesem Interface
# Beispiel: eth0 (Standard LXC), ens18 (Proxmox), enp0s3 (VirtualBox)
# Prüfen mit: ip addr show
NETWORK_INTERFACE

# ============================================
# N8N SERVICE CONFIGURATION
# ============================================

# Port auf dem n8n läuft
# Beispiel: 5678 (Standard), 8080, 3000
# Muss mit dem Port in n8n's docker-compose übereinstimmen
N8N_PORT

# Name unter dem n8n in Consul registriert wird
# Andere Services finden n8n via: n8n.service.consul
# Beispiel: n8n, n8n-prod, workflow-engine
# Regel: Klein geschrieben, keine Leerzeichen
N8N_SERVICE_NAME

# Health Check Endpoint für n8n
# Consul ruft diesen Pfad auf um zu prüfen ob n8n gesund ist
# Beispiel: /healthz, /health, /api/health
# n8n Standard: /healthz
N8N_HEALTH_PATH=/healthz

# ============================================
# POSTGRES SERVICE CONFIGURATION
# ============================================

# Port auf dem PostgreSQL läuft
# Beispiel: 5432 (Standard), 5433
POSTGRES_PORT

# Name unter dem PostgreSQL in Consul registriert wird
# Andere Services finden DB via: postgres.service.consul
# Beispiel: postgres, postgres-n8n, database
# Bei mehreren DBs: postgres-n8n, postgres-langflow
POSTGRES_SERVICE_NAME

# PostgreSQL Passwort
# Beispiel: GeheimesPasswort123!, $(vault read ...)
# WICHTIG: Sicheres Passwort verwenden! Nicht ins Git committen!
POSTGRES_PASSWORD

# ============================================
# SERVICE TAGS (optional)
# ============================================

# Tags für Service-Kategorisierung in Consul
# Comma-separated Liste von Tags
# Beispiel: lxc-n8n, production, team-a, v2.0
# Verwendung: Für Filtering in Consul Queries
# consul catalog services -tags "production"
SERVICE_TAGS